<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="cloud-watch-appender.xml"/>

    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-4relative] %green([%thread]) %highlight(%-5level) %boldWhite([%C.%M:%yellow(%L)]) - %msg%n"/>
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %5level %logger - %msg%n"/>
    <property name="DISCORD_LOG_PATTERN" value="%d{HH:mm:ss} [%thread] [%-5level] %logger{36} - %msg%n```%ex{full}```"/>

    <springProperty name="PROFILE" source="spring.profiles.active"/>
    <springProperty name="AWS_CLOUDWATCH_ACCESS_KEY" source="cloud.aws.cloud-watch.access-key"/>
    <springProperty name="AWS_CLOUDWATCH_SECRET_KEY" source="cloud.aws.cloud-watch.secret-key"/>
    <springProperty name="DISCORD_WEBHOOK_URL" source="logging.discord.webhook-url"/>

    <!--local-->
    <springProfile name="local">
        <include resource="console-appender.xml"/>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!--dev-->
    <springProfile name="dev">
        <include resource="file-dev-appender.xml"/>
        <include resource="discord-appender.xml"/>

        <root level="INFO">
            <appender-ref ref="FILE-DEV"/>
            <appender-ref ref="CLOUD-WATCH"/>
            <appender-ref ref="ASYNC_DISCORD"/>
        </root>

        <logger level="DEBUG" name="org.hibernate.SQL">
            <appender-ref ref="FILE-DEV"/>
            <appender-ref ref="CLOUD-WATCH"/>
        </logger>

        <logger level="TRACE" name="org.hibernate.type.descriptor.sql.BasicBinder">
            <appender-ref ref="FILE-DEV"/>
            <appender-ref ref="CLOUD-WATCH"/>
        </logger>
    </springProfile>

    <!--prod-->
    <springProfile name="prod">
        <include resource="file-prod-appender.xml"/>
        <include resource="discord-appender.xml"/>

        <root level="ERROR">
            <appender-ref ref="FILE-PROD"/>
            <appender-ref ref="CLOUD-WATCH"/>
            <appender-ref ref="ASYNC_DISCORD"/>
        </root>
    </springProfile>

</configuration>